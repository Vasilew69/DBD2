<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="utf-8">
	<meta http-equiv="X-UA-Compatible" content="IE=edge">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<title>500 - Server Error</title>
    <style>
        * {
            background-color: #000000;
        }
        .center {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 70vh;
        }

        .error-page {
            text-align: center;
            background-color: #1e1e2f;
            padding: 40px;
            border-radius: 16px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.3);
            color: #fff;
            max-width: 600px;
            width: 100%;
            margin: auto;
            animation: fadeIn 0.6s ease-in-out;
        }

        .error-code {
            background-color: #1e1e2f;
            font-size: 3rem;
            font-weight: 700;
            color: #f87171;
            margin-bottom: 10px;
        }

        .error-message {
            background-color: #1e1e2f;
            font-size: 1.8rem;
            margin-bottom: 15px;
        }

        .error-description {
            background-color: #1e1e2f;
            font-size: 1rem;
            margin-bottom: 30px;
            color: #ccc;
        }

        .btn {
            background-color: #1e1e2f;
            margin: 0 10px;
            padding: 10px 18px;
            font-size: 1rem;
            border-radius: 8px;
            transition: 0.2s ease-in-out;
        }

        .btn-primary {
            background-color: #3b82f6;
            border: none;
            color: white;
        }

        .btn-primary:hover {
            background-color: #2563eb;
            transform: scale(1.05);
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
</head>
<body>
    <div id="main">
        <div class="row">
            <div class="col-12">
              <div class="center">
                <div class="error-page">
                    <h1 class="error-code">500</h1>
                    <h2 class="error-message">Internal Server Error</h2>
                    <p class="error-description">
                        <h2 class="error-description">An unexpected error occurred on the server.</h2>
                        <% if (message) { %>
                          <p class="error-description"><strong class="error-description">Error:</strong> <%= message %></p>
                        <% } %>
                    </p>
                    <a class="btn btn-primary" href="javascript:history.back()">Go Back</a>
                    <a class="btn btn-primary" href="/home">Go Home</a>
                  </div>
              </div>
            </div>
          </div>
    </div>
</body>
</html>
